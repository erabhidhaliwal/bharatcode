---
description: Push changes to the GitHub repository
---
// turbo-all
1. Stage all changes
   `git add .`
2. Commit changes with a descriptive message
   `git commit -m "Update code: [describe changes]"`
3. Push to the main branch
   `git push origin main`
